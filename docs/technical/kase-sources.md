# Источники данных KASE — реестр проверенных эндпоинтов

**Дата проверки: 13 августа 2026.** Все запросы ниже выполнены реально, с
живого сайта. Ни один эндпоинт не выдуман и не восстановлен по памяти.

## Как эти эндпоинты были найдены

kase.kz — это Angular-приложение с server-side rendering. Каждая страница
содержит `<script id="ng-state" type="application/json">` — состояние
transfer state, в котором Angular сохраняет **URL и тело каждого HTTP-запроса**,
выполненного при рендеринге страницы.

Метод, который можно повторить:

```bash
curl -s https://kase.kz/ru/bonds/ -o page.html
python - <<'EOF'
import re, json
h = open('page.html', encoding='utf-8').read()
state = json.loads(re.search(r'<script id="ng-state"[^>]*>(.*?)</script>', h, re.S).group(1))
for key, entry in state.items():
    if isinstance(entry, dict) and 'u' in entry:
        print(entry.get('s'), entry.get('u'))   # HTTP-статус и URL
EOF
```

Так был получен список ниже. Никакого обхода защиты: это те же публичные
запросы, которые делает браузер обычного посетителя.

> **Важно про завершающий слэш.** Без него KASE отвечает `301` на слэш-форму.
> Клиенты, которые не переносят query string через редирект, теряют фильтры.
> Всегда обращайтесь к слэш-форме.

---

## PUBLIC — доступно без авторизации, используется в MVP

Общее для всех: `GET`, ответ `application/json`, авторизация **не требуется**,
базовый URL `https://kase.kz`.

### 1. Каталог инструментов

| | |
|---|---|
| **Что это** | Полный список инструментов биржи, включая все облигации |
| **Endpoint** | `/api/instruments/securities/?sec_type=bond` |
| **Ответ** | JSON-массив, ~13 МБ без фильтра, **2 183 облигации** с `sec_type=bond` |
| **Поля** | `code` (тикер), `org_code` (код эмитента), `org_name_*`, `currency_type`, `repayment_start_date` (погашение), `volume_release` / `volume_release_number` (объем выпуска и число бумаг — их отношение дает номинал), `best_bid`, `best_offer`, `price`, `dealcnt`, `ytm`, `dtm`, `board_*`, `fin_sec_*` (сектор), `market_makers`, `cnt_mm`, `liquid_class`, `t_plus` |
| **Чего НЕТ** | **ISIN, ставка купона, даты купонов** — только в эндпоинте №2 |
| **Пагинация** | `?page=1&per_page=12` переключает ответ в форму `{count, next, previous, results}` |
| **Частота** | 1 раз в сутки. Состав рынка меняется медленно |
| **Ограничения** | Ответ большой: не запрашивать на каждый пользовательский запрос |
| **MVP** | Да — это основа каталога |

Дополнительно: `?sec_type=gsec` дает 3 782 государственные бумаги.

### 2. Параметры выпуска — ключевой эндпоинт

| | |
|---|---|
| **Что это** | Полная карточка одной облигации |
| **Endpoint** | `/api/instruments/bonds/{TICKER}/` |
| **Поля** | Внутри `characteristics`: **`isin`**, **`cfi`**, **`coupon_rate`** (% годовых), `coupon_type_*`, `nominal_value_*`, `one_lot`, `registred_volume`, `issued_volume`, `market_volume`, `prev_coupon_start_date`, **`next_coupon_start_date`**, `next_coupon_fixation_date`, `maturity_start_date`, `maturity_end_date`, `excluded_at`, `is_index`, `indexation_method_*`, `quatation_system_*` («чистая цена»), `quatation_unit_*` («процент от номинала»), `registrar_*`, `coupon_description_*` (свободный текст с периодом обращения) |
| **Частота** | При добавлении выпуска; далее раз в неделю (меняется `next_coupon_*`) |
| **Ограничения** | Один запрос на выпуск. ~970 активных облигаций → синхронизация порциями с ограничением параллелизма |
| **MVP** | Да — без него нет ни ISIN, ни купона, ни графика выплат |

Проверенный пример (`BRKZb14`): `isin=KZ2C00004273`, `cfi=DBFUFR`,
`coupon_rate=11.0`, `nominal=1000.0`, `one_lot=1.0`,
`prev_coupon=2026-06-18`, `next_coupon=2026-12-18`, `maturity=2030-06-18`.

**Частота купона не публикуется напрямую.** Основной способ — шаг
опубликованного графика (эндпоинт 2a), запасной — разрыв между
`prev_coupon_start_date` и `next_coupon_start_date` (`infer_coupon_frequency()`).
Запасной способ не работает примерно для каждого пятого торгуемого выпуска,
поэтому график приоритетнее.

**CFI (ISO 10962) — единственный машиночитаемый источник** признаков
`secured` / `subordinated` / `callable` / `putable`. `DBFUFR` = долговая,
облигация, фиксированный купон, необеспеченная, фиксированное погашение,
именная. Разбор в `parse_cfi()`; нераспознанная позиция дает `None`, а не
`False`.

### 2a. График купонных выплат

| | |
|---|---|
| **Что это** | Официальный график купонов выпуска за весь срок обращения |
| **Endpoint** | `/api/instruments/coupon-payments/{TICKER}/` |
| **Как найден** | В JS-бандле сайта как `getCouponPayments()` |
| **Поля** | `fixation_date` (дата фиксации ставки), **`start_date` — дата выплаты**, `end_date` (конец окна расчетов, ~14 дней), `rate`, `index_rate`, **`total_rate`** — применяемая годовая ставка периода |
| **Частота** | Раз в неделю; для плавающих купонов — после каждой фиксации |
| **MVP** | Да |

Проверено: `BRKZb14` — 20 периодов с 2020 по 2030; `MFOKb21` — 36 ежемесячных;
`BTRKb24` — 5 годовых.

**Почему это важнее, чем кажется.** До нахождения этого эндпоинта график
восстанавливался откатом от даты погашения, и у плавающих выпусков будущие
купоны считались по сегодняшней ставке. Теперь:

* потоки помечаются `is_estimated=false` — это факт, а не проекция;
* у плавающих и индексируемых бумаг берется ставка **каждого** периода;
* из шага графика надежно выводится частота выплат.

### 3. Итоги торгов — цены и доходности

| | |
|---|---|
| **Что это** | Результаты последней торговой сессии по облигациям |
| **Endpoint** | `/api/trade-results/bonds/` |
| **Ответ** | ~139 записей — только бумаги, по которым были заявки или сделки |
| **Поля** | `current_bid`, `current_offer` (**ask!**), `openprice`, `max_price`, `min_price`, `last_deal_price`, `avg_price`, `last_deal_date`, `dealcnt`, `vol`, `volkzt`, `volusd`, `dtm`, `basis` (база начисления, `360`), `board`, и зеркальные наборы `dirty_*` и `*_dohod` (доходности к bid/ask/last/avg) |
| **Частота** | Несколько раз в день; данные — итоги сессии, не поток реального времени |
| **MVP** | Да — именно этот эндпоинт делает возможным честное ценообразование |

> ### ⚠ Две разные шкалы в одном ответе
>
> Проверено на 22 выпусках: **`clean`-поля — процент от номинала, а все
> `dirty_*` поля — деньги за одну бумагу в валюте выпуска.**
>
> Пример `BRKZb14`: `last_deal_price = 88.5775` (% от номинала),
> `dirty_last_price = 902.58` (тенге при номинале 1 000).
>
> НКД = `dirty_money − clean_pct/100 × nominal` = 16.81 ₸ = 1.68 % номинала.
>
> Смешать шкалы = ошибиться в цене в ~10 раз. `KasePublicApiProvider.get_quotes()`
> приводит все к проценту от номинала и **требует номинал**; без него
> `dirty_price` и `accrued_interest` возвращаются как `null`, а не в неверной шкале.

Доходности (`*_dohod`) публикуются в процентах годовых и переводятся в
десятичную дробь при сохранении.

### 4. Эмитенты

| | |
|---|---|
| **Endpoints** | `/api/companies/issuers/` (список, ~304 КБ) и `/api/companies/issuers/{CODE}/` |
| **Поля** | `code`, `full_name_*`, `short_name_*`, `website_*`, **`is_financial`** (банк или нет — определяет модель Credit Score), **`list_category`** (`official` / `alternative` — уровень листинга), `sec_count`, `lei`, `address_*`, `primary_activity_*`, `capitalization` |
| **Частота** | Раз в неделю |
| **MVP** | Да |

### 5. Эмитенты в дефолте

| | |
|---|---|
| **Endpoint** | `/api/companies/defaulted-issuers/` |
| **Ответ** | **33 эмитента** на дату проверки |
| **Значение** | Жесткий официальный кредитный факт. Самый ценный кредитный вход, который дает публичный API |
| **Частота** | Ежедневно |
| **MVP** | Да |

### 6. Финансовая отчетность

| | |
|---|---|
| **Endpoint** | `/api/companies/fin-data/{CODE}/` |
| **Ответ** | Поквартальный ряд; для `BRKZ` — 99 периодов |
| **Поля** | `change_date`, `units` (`thnd` — тысячи!), `currency`, `net_profit`, `gross_income`, `own_capital`, `volume_sale`, `aggregate_assets`, `total_liabilities`, `authorized_capital`, `audited`, `roe`, `roa`, `ros` |
| **Частота** | После публикаций отчетности (ежеквартально) |
| **MVP** | Да, с оговоркой ниже |

> **Ограничение, определяющее модель Credit Score.** В этом источнике **нет**
> EBITDA, процентных расходов, разбивки долга и денежных потоков. Поэтому
> **Debt/EBITDA, Net Debt/EBITDA и Interest Coverage из публичных данных KASE
> не вычисляются** — они остаются `null`, и `confidence` кредитной оценки
> понижается. Доступны: leverage как «обязательства / капитал»,
> equity/assets, ROA, ROE, ROS, динамика выручки и прибыли, факт аудита.
>
> Мы сознательно **не** подставляем `total_liabilities` вместо `total_debt`:
> обязательства включают кредиторскую задолженность, а у банков — депозиты
> клиентов. В модели это отдельная колонка `financial_statements.total_liabilities`.

### 7. Документы эмитента

| | |
|---|---|
| **Endpoint** | `/api/companies/documents/?language=ru&org_code={CODE}` |
| **Поля** | `name`, `link` (относительный путь к PDF), `date0`, `year`, `category_id`, `org_code`, `org_type` |
| **MVP** | P2 — ссылки собираем, содержимое PDF не разбираем |

> **Обязательные параметры.** Без `language` — `400`. Без `org_code` эндпоинт
> отдает **все документы биржи, ~50 МБ**. Коллектор обязан всегда передавать
> `org_code`; с фильтром ответ для `BRKZ` — 864 КБ, 1 552 документа.

### 8. Индикаторы рынка — бенчмарки и кривая ГЦБ

| | |
|---|---|
| **Endpoint** | `/api/indicators/` |
| **Ответ** | 220 индикаторов, из них 119 действительно обновляются |
| **Частота** | Внутридневная |
| **MVP** | Да — источник безрисковой ставки для credit spread |

Ключевые коды, проверенные значения на 12.08.2026:

| Код | Смысл | Значение |
|---|---|---|
| `KZGB_Ys` | доходность ГЦБ до 1 года | 15.86 % |
| `KZGB_Ym` | доходность ГЦБ 1–5 лет | 15.14 % |
| `KZGB_Yl` | доходность ГЦБ от 5 лет | 14.32 % |
| `KZGB_Y` | ГЦБ без сегментации | 14.52 % |
| `KASE_BMY` | индикатор доходности корпоративных облигаций | 16.38 % |
| `KASE_TO` | TONIA (овернайт) | 15.76 % |
| `USDKZT_DAY` | курс USD/KZT | 465.13 |
| `KASE_KZ3_Y` / `KASE_KZ3_DUR` | доходность и дюрация индекса KZT-облигаций | 15.25 % / 2.38 |

### 8a. Состав индексов — кривая ГЦБ по бумагам

| | |
|---|---|
| **Endpoint** | `/api/indicators/kzgb/representative-list/` |
| **Ответ** | **57 гособлигаций**, каждая с `duration`, `duration_mod`, `dohod`, `price_clean_percent`, `accrued_interest`, `dtm`, `capit`, `nominal`, `total_coupon` |
| **Аналог для корпоративных** | `/api/indicators/kase-b/{INDEX_CODE}/representative-list/`, например `KASE_BMY` |
| **Частота** | Ежедневно |
| **MVP** | Да |

Из этих точек строится настоящая срочная структура (взвешивание по
капитализации, узлы 0.25–20 лет). Проверенный результат на 13.08.2026:

```text
 0.25y 15.94%   1y 15.71%   3y 14.99%   7y 14.06%
 0.50y 15.79%   2y 15.42%   5y 14.34%  10y 13.77%
```

Credit spread берется к бенчмарку **сопоставимой дюрации**, а не к одному
числу на весь рынок. Пишется в таблицу `yield_curves` (`curve_code=KZ_GOV`)
командой `sync-kase-all`.

Для валют, по которым кривой нет (например USD-выпуски), `credit_spread`
остается `null` — сравнивать доходность USD-бумаги с тенговой кривой
некорректно.

### 9. Первичные размещения

| | |
|---|---|
| **Endpoint** | `/api/instruments/bonds/recent-placements/` |
| **Поля** | `code`, `isin`, `date0`, `finish_date`, `cupon`, `ytm`, `volkzt`, `org_code`, `part` |
| **MVP** | P2 |

Соседний `/api/instruments/bonds/announcements/` на дату проверки отдает `[]`.

---

## LICENSED / API — требует договора

| Источник | Статус |
|---|---|
| Контрактный API KASE (`KASE_API_URL` + `KASE_API_KEY`) | Реализован как `KaseApiProvider`. Карты `ENDPOINTS` и `FIELD_MAP` — заготовка под ваш договор, **не документированный контракт биржи**. Требует заключения договора с KASE |
| Поток котировок в реальном времени | Публичный API отдает **итоги сессии**, не тик-поток. Реальное время — только по договору |
| Глубина стакана (order book depth) | Публично не публикуется. Доступны только лучшие bid/ask |

**Ничего из этого не обходится.** Точка подключения оставлена
(`KASE_DATA_MODE=official_api`), фиктивных эндпоинтов не создано.

---

## NOT AVAILABLE — публично отсутствует

> ### Как проверялось отсутствие
>
> Список ниже — это **не** «мы не нашли эндпоинт», а «эндпоинта не существует».
> Из JS-бандла сайта извлечены **все** шаблоны путей, которыми пользуется сам
> KASE:
>
> ```bash
> # 49 chunk-файлов приложения
> grep -oE 'chunk-[A-Z0-9]{8}' main-*.js | sort -u          # список чанков
> # затем по каждому:
> grep -oE '(apiService|cmsService)\.(get|post)\(\s*[`"'"'"']([^`"'"'"']+)' chunk-*.js
> grep -oE 'this\.url\s*=\s*["`]([a-z0-9/_-]+)["`]' chunk-*.js
> ```
>
> Полученный список путей — исчерпывающая поверхность API. Того, чего в нем
> нет, публично не существует.

| Данные | Что делаем |
|---|---|
| **Кредитные рейтинги агентств** | Эндпоинта нет. Единственное упоминание «rating» во всем бандле — форма заявки на листинг, где эмитента *спрашивают*, есть ли у него рейтинг. `get_ratings()` возвращает `[]`; рейтинг **никогда не выдумывается**, вместо этого снижается `confidence` кредитной оценки |
| **Глубина заявок (order book)** | Эндпоинта нет; публикуются только лучшие bid/ask. Слова `orderbook`, `depth`, `bid_volume` в бандле отсутствуют (`Depth` встречается только во внутренностях Angular). Поэтому большие суммы получают `liquidity_warning`, а не выдуманную цену исполнения |
| **Тиковый журнал сделок по облигациям** | `last-deals` существует **только** для акций: `/api/instruments/shares/{CODE}/last-deals/` → `200`, тот же путь для облигаций → `404`. По облигациям доступен только агрегат сессии (VWAP + число сделок), и он возвращается именно как агрегат |
| **EBITDA, процентные расходы, денежные потоки** | В `fin-data` этих строк нет, слово `ebitda` в бандле не встречается. Поэтому **Debt/EBITDA и Interest Coverage не вычисляются** и остаются `null`. Доступное: leverage «обязательства/капитал», equity/assets, ROA, ROE, ROS, динамика, факт аудита. См. раздел 6 |
| **График досрочного погашения (call/put)** | Факт наличия опциона виден из CFI, но даты и цены исполнения не публикуются. `calculate_ytc`/`calculate_ytp` возвращают `null` без даты и **не** выдают YTM под чужим именем |

### Ранее считалось недоступным — найдено и внедрено

| Данные | Решение |
|---|---|
| **График купонных выплат** | **Найден эндпоинт `/api/instruments/coupon-payments/{TICKER}/`** (в бандле — `getCouponPayments()`). Дает все периоды за весь срок жизни выпуска: `fixation_date`, `start_date` (дата выплаты), `end_date`, `rate`, `index_rate`, `total_rate`. Схема больше **не восстанавливается**: потоки помечаются `is_estimated=false`, а для плавающих купонов используется ставка **каждого периода**, а не сегодняшняя |
| **Частота купона** | Выводится из шага опубликованного графика (медиана разрывов). Это устранило реальный баг: у выпусков, где `prev/next_coupon_start_date` не дают шаг (ASKQb2, BIGDb7/8/12, BTRKb24, FPSTb11), частота была `null`, бумага классифицировалась как **дисконтная**, и НКД по облигации с купоном 20 % считался равным **нулю** |
| **Кривая доходности ГЦБ** | `/api/indicators/kzgb/representative-list/` дает **57 гособлигаций** с индивидуальной дюрацией и доходностью. Из них строится настоящая срочная структура (8 узлов, 15.94 % → 13.77 %) вместо трех грубых сегментов, и credit spread берется к бенчмарку сопоставимой дюрации |
| **Поиск** | `/api/search/suggestions/?query=` — собственный поисковый индекс биржи, вместо сканирования 13-мегабайтного каталога |
| **Дневная история котировок за 2 года** | **Найден эндпоинт `/tv-charts/securities/history?symbol={TICKER}&resolution=D&from=&to=`** — публичный UDF-фид, который страница инструмента сама грузит для своего графика. Отдает OHLCV по каждому торговому дню (≈492 бара за 2 года на бумагу) одним запросом вместо пагинации отрендеренной таблицы, которая давала лишь ~5 дней. Отвечает JSON под `content-type: text/plain`, поэтому тело парсится явно. Сопутствующие: `/tv-charts/securities/{config,time,symbols}`. Внедрено в `app/services/backfill/chart_api.py` |
| **EBITDA, процентные расходы, денежные потоки** | Нет в `fin-data`. См. ограничение в разделе 6 |
| `robots.txt` | Отдает `404` — файла нет |

---

## Инфляция — официальный первичный источник

| | |
|---|---|
| **Издатель** | Бюро национальной статистики АСПР РК — `stat.gov.kz` |
| **Страница** | `/ru/industries/economy/prices/publications/` |
| **Бюллетень** | «Инфляция в Республике Казахстан (месяц год)» |
| **Проверенное значение** | **10.2 % годовых, июль 2026** ([публикация 509832](https://stat.gov.kz/ru/industries/economy/prices/publications/509832/)) |
| **Формат** | HTML + XLSX. **JSON API отсутствует** |
| **Частота** | Ежемесячно |

Реализация — `StatGovInflationCollector`: читает индекс публикаций, находит
свежий бюллетень по инфляции (`<a class="release-title">`), открывает его и
извлекает значение из формулировки «…составила 10,2 %».

Ограничения приняты осознанно:

* парсер привязан к верстке stat.gov.kz и сломается при редизайне — тогда он
  **падает явно и не трогает сохраненное значение**: устаревшее, но настоящее
  число лучше свежего выдуманного;
* значение вне диапазона 0–100 % годовых считается ошибкой разбора, а не новостью;
* ручное переопределение (`python scripts/kase.py set-inflation 10.2`) имеет
  приоритет и существует именно на случай поломки парсера.

Национальный банк РК дополнительно отдает официальные курсы валют:
`https://nationalbank.kz/rss/rates_all.xml` (проверено, `200`).

---

## Сводная таблица частоты обновления

| Данные | Эндпоинт | Частота |
|---|---|---|
| Каталог | `/api/instruments/securities/?sec_type=bond` | раз в сутки |
| Параметры выпуска | `/api/instruments/bonds/{TICKER}/` | раз в неделю |
| График купонов | `/api/instruments/coupon-payments/{TICKER}/` | раз в неделю |
| Котировки и сделки | `/api/trade-results/bonds/` | несколько раз в день |
| Индикаторы | `/api/indicators/` | несколько раз в день |
| Кривая ГЦБ по бумагам | `/api/indicators/kzgb/representative-list/` | раз в сутки |
| Эмитенты | `/api/companies/issuers/` | раз в неделю |
| Дефолты | `/api/companies/defaulted-issuers/` | раз в сутки |
| Отчетность | `/api/companies/fin-data/{CODE}/` | после публикаций |
| Документы | `/api/companies/documents/?org_code=…` | раз в неделю |
| Инфляция | stat.gov.kz | раз в месяц |

## Вежливость к источнику

* `asyncio.Semaphore` ограничивает параллелизм (по умолчанию 4 запроса);
* таймауты на каждом запросе, сетевая ошибка возвращается как данные, а не как падение;
* каталог и параметры выпусков синхронизируются по расписанию, а не по запросу пользователя;
* `ENRICH_BUDGET` ограничивает число карточек выпусков за один прогон.

## Аудит

Каждый ответ источника сохраняется в `raw_kase_data` целиком: URL, HTTP-статус,
content-type, тело, SHA-256 тела, длительность запроса, момент загрузки и
`data_mode`. Любое число на экране прослеживается до исходных байтов.
