# Runtime dependencies for the AI system (dataset, retrieval, inference).
# Training needs ai/requirements-training.txt on top of this.

numpy>=1.26
pyyaml>=6.0
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
httpx>=0.28

# Optional document parsers (§10). Each format degrades to "unsupported" with
# a clear message when its parser is absent, rather than failing a build.
pypdf>=5.1          # PDF
openpyxl>=3.1       # XLSX/XLS
python-docx>=1.1    # DOCX

# Open-weight embeddings (§25). Without it, ai/embeddings falls back to the
# deterministic hashing embedder, which keeps the pipeline runnable but is not
# a production retrieval option.
# sentence-transformers>=3.3
